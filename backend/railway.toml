[build]
builder = "NIXPACKS"
buildCommand = "pnpm install && pnpm run db:migrate"

[deploy]
startCommand = "pnpm start"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
